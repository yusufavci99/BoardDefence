using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class LevelDatax : MonoBehaviour
{
 
    //private void Awake() {
    //    if (levelData == null) {
    //        levelData = this;
    //    } else {
    //        Destroy(this);
    //    }
    //}

    //public Stack<EnemyData> GetEnemies(int level) {
    //    List<EnemyData> enemiesList = RemoveInt(levels[level - 1].enemyAndCounts);
    //    Shuffle(enemiesList);
    //    return new Stack<EnemyData>(enemiesList);

    //}
    

    //public static void Shuffle<T>( IList<T> list) {
    //    int n = list.Count;
    //    while (n > 1) {
    //        n--;
    //        int k = Random.Range(0, n + 1);
    //        T value = list[k];
    //        list[k] = list[n];
    //        list[n] = value;
    //    }
    //}
}
